import { Mahafunc } from "./Mahafunc";
export declare class Munger extends Mahafunc {
    get elementName(): string;
    /**
     * For _Munger_ classes, the `process` function has access
     * to the entire HTML for the page.  Hence, it is passed
     * both `$` for the whole page, and `$element` matching
     * a specific element on the page.
     *
     * @param $
     * @param $element
     * @param metadata
     * @param setDirty
     * @param done
     */
    process($: any, $element: any, metadata: any, setDirty: Function, done?: Function): Promise<void>;
    processAll($: any, metadata: any, setDirty: Function): Promise<void>;
}
//# sourceMappingURL=Munger.d.ts.map